<nav class="navbar navbar-dark navbar-static-top{{#is "home"}} top{{/is}}"><!-- bg-inverse -->
    <div class="container">
        <div class="test">
            <a class="navbar-brand" href="/">
                <img src="{{@blog.logo}}" width="60" height="30" class="d-inline-block align-top" alt="">
                {{@blog.title}}
            </a>
            <span class="navbar-text float-xs-left text-muted" style="color: #dedede!important;">
                {{@blog.description}}
            </span>

        </div>
        <button class="navbar-toggler hidden-lg-up float-xs-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation"></button>
        <div class="collapse navbar-toggleable-md" id="navbarResponsive">
            <ul class="nav navbar-nav float-xs-right">

                {{#foreach navigation}}
                    <li class="nav-item {{#if current}}active{{/if}}">
                        <a class="nav-link" href="{{url absolute="true"}}">{{label}} {{#if current}}<span class="sr-only">(current)</span>{{/if}}</a>
                    </li>
                {{/foreach}}
                {{#if @labs.subscribers}}
                    <li class="nav-item">
                        <a class="nav-link subscribe-button" href="{{@blog.url}}/subscribe/">Subscribe</a>
                    </li>
                {{else}}
                    <li class="nav-item">
                        <a class="nav-link subscribe-button icon-feed" href="{{@blog.url}}/rss/">Subscribe</a>
                    </li>
                {{/if}}
            </ul>
        </div>
    </div>
</nav>